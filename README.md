# 🛡️ OverTheWire: Bandit Linux Security Audit
**Developer Workspace:** `debmalya-lab`  
**Objective:** Practical verification of Linux system architecture, network reconnaissance, and cryptographic baselines for advanced cybersecurity profiles.

---

### 🚀 Technical Core Accomplishments
* Conquered **20 complete tiers** of the OverTheWire Bandit playground.
* Mastered low-level data forensics, multi-property file routing, and background pipeline streams.
* Established bulletproof terminal command-line muscle memory.

---

### 🛠️ Key Architectural Write-Ups

#### 🔍 1. Multi-Property Data Forensics (Levels 1–12)
* **Concepts:** File attribute filtering, byte-size targeting, duplicate isolation, and recursive archive deconstruction.
* **Core Tools Mastered:** `find -size`, `sort | uniq -u`, `xxd -r`, `file`, `gunzip`, and `tar -xf`.
* **Logic:** Extracted unique security tokens by stripping away messy duplicates and peeling back complex nested compression matrices.

#### 📡 2. Network Reconnaissance & Port Auditing (Levels 13–16)
* **Concepts:** Passwordless cryptographic authentication, live network stream connections, secure client handshakes, and multi-port vulnerability scanning.
* **Core Tools Mastered:** `ssh -i`, `nc`, `openssl s_client -connect`, and `nmap -sV`.
* **Logic:** Bypassed standard credential requirements using private RSA keycards, opened live pipelines to server ports, and ran automated scanners across 1,000 doors to isolate secure encrypted gates.

#### 👑 3. Privilege Escalation & Automation (Levels 17–20)
* **Concepts:** Configuration diff audits, TTY/auto-logout shell bypasses, SUID master permission inheritance, and automated background process subshells.
* **Core Tools Mastered:** `diff`, `ssh` command injections, `./` local binaries, and `&` background ampersand threads.
* **Logic:** Identified modified parameters side-by-side, forced remote command executions before environmental disconnections, utilized elevated signature stamps to read locked vaults, and deployed background network listeners on autopilot.
